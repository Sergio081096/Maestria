\chapter{Marco Teórico y Conceptual}

El fundamento teórico del sistema propuesto consiste en una arquitectura de planeación jerárquica. Se introduce el funcionamiento de los modelos de lenguaje grande y se detalla la integración de traductores de lenguaje natural con sistemas basados en reglas, estableciendo las bases tecnológicas para la implementación del sistema híbrido CLIPS-ChatGPT/Qwen y su uso en la arquitectura ViRoot \textit{(Virtual Real Robot)} descrita a continuación (ver Figura \ref{fig:viroot}).

\subsection*{Arquitectura del Sistema}

La arquitectura VIRBOT de Justina se organiza en cuatro capas principales:

\begin{itemize}
    \item \textbf{Capa de Entradas}
    \begin{itemize}
        \item Procesamiento de datos de sensores internos y externos.
        \item Aplicación de técnicas de reconocimiento de patrones.
        \item Generación del estado del entorno.
    \end{itemize}
    \item \textbf{Capa de Planificación}
    \begin{itemize}
        \item Validación mediante gestión del conocimiento.
        \item Reconocimiento de situaciones y activación de objetivos.
        \item Planificación de secuencias de operaciones físicas.
    \end{itemize}
    \item \textbf{Capa de Gestión del Conocimiento}
    \begin{itemize}
        \item Mapas del entorno creados con técnicas SLAM.
        \item Sistema de localización basado en filtro de Kalman.
        \item Sistema basado en reglas CLIPS para representación del conocimiento.
    \end{itemize}
    \item \textbf{Capa de Ejecución}
    \begin{itemize}
        \item Ejecución y verificación de planes de movimiento.
        \item Procedimientos predefinidos representados como máquinas de estado.
        \item Integración de procedimientos para generar planes complejos.
    \end{itemize}
\end{itemize}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{Figures/virbot.png}
    \caption{Arquitectura ViRoot.}
    \label{fig:viroot}
\end{figure}

\subsection*{Arquitectura Funcional de ViRbot}

Esta arquitectura organiza los módulos funcionales del robot de manera interconectada, lo que permiten integrar la percepción, comprensión del lenguaje, planeación simbólica y control físico del robot. La Figura \ref{fig:viroot2} muestra la interacción entre los distintos subsistemas del robot Justina.

Dentro de la arquitectura se distinguen los cuatro grupos principales: percepción, comprensión e interacción, planificación y supervisión, y control de hardware.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{Figures/virbot.jpg}
    \caption{Arquitectura Funcional ViRoot.}
    \label{fig:viroot2}
\end{figure}

\subsubsection*{Módulos de Percepción}

Los módulos de percepción permiten al robot obtener información del entorno físico mediante sensores y algoritmos de interpretación.

\begin{itemize}

\item \textbf{Vision:} Este módulo procesa información proveniente de cámaras y sensores visuales \textit{(nubes de puntos)}. Permite la detección y reconocimiento de objetos, personas y características del entorno. La información visual es utilizada principalmente por el sistema de navegación y por el planificador de tareas para tomar decisiones sobre la interacción con el entorno.

\item \textbf{Cartographer:} El módulo Cartographer implementa algoritmos de \textbf{SLAM} \textit{(Simultaneous Localization and Mapping)} para construir mapas del entorno y estimar la posición del robot dentro de ellos. Este módulo proporciona información fundamental para la navegación autónoma.

\item \textbf{Speech Recognition:} Este módulo procesa las señales de audio capturadas por los micrófonos del robot y las convierte en texto. El resultado es utilizado por el sistema de \textit{Natural Language Understanding} para interpretar instrucciones del usuario.

\end{itemize}

\subsubsection*{Módulos de Comprensión e Interacción}

Estos módulos permiten que el robot interactúe con los humanos mediante lenguaje natural.

\begin{itemize}

\item \textbf{Natural Language Understanding (NLU):} Este componente interpreta las instrucciones del usuario en lenguaje natural. Utiliza modelos lingüísticos y reglas semánticas para transformar frases en representaciones estructuradas que puedan ser utilizadas por el sistema de planificación.

\item \textbf{LLMs:} Los Large Language Models (LLMs) se emplean como complemento para mejorar la interpretación semántica de las instrucciones. Estos modelos permiten generar representaciones simbólicas o estructuras intermedias que posteriormente son procesadas por el sistema de planificación basado en reglas.

\item \textbf{Speech Generation:} Este módulo permite al robot generar respuestas habladas. Convierte texto generado por el sistema en señales de voz, facilitando la interacción natural con los usuarios.

\end{itemize}

\subsubsection*{Módulos de Planificación y Gestión del Conocimiento}

Estos módulos constituyen el núcleo de la toma de decisiones del sistema.

\begin{itemize}

\item \textbf{CLIPS Knowledge Base:} La base de conocimiento implementada en CLIPS almacena hechos y reglas que representan el conocimiento del entorno, los objetos, las tareas y las capacidades del robot. Esta base de conocimiento se actualiza continuamente conforme el robot percibe el entorno.

\item \textbf{CLIPS Planner:} Este módulo utiliza reglas de producción para generar planes de acción a partir de objetivos definidos. El planificador determina qué acciones deben ejecutarse y en qué orden para alcanzar una meta específica.

\item \textbf{Simple Task Planner State Machine:} Este componente implementa una máquina de estados que coordina la ejecución de tareas simples. Funciona como un nivel intermedio entre la planificación simbólica y la ejecución de acciones físicas.

\item \textbf{Supervisor:} El supervisor monitorea la ejecución de los planes y gestiona errores o situaciones inesperadas. También se encarga de coordinar la interacción entre los distintos módulos del sistema.

\item \textbf{Executor:} El ejecutor recibe los planes generados por el planificador y los convierte en comandos concretos para los diferentes actuadores del robot.

\end{itemize}

\subsubsection*{Módulos de Control del Robot}

Estos módulos controlan directamente los sistemas físicos del robot.

\begin{itemize}

\item \textbf{Navigation:} El sistema de navegación calcula trayectorias y controla el desplazamiento del robot utilizando información del mapa y de los sensores.

\item \textbf{Mobile Base:} Este módulo controla la base móvil del robot, permitiendo su desplazamiento en el entorno.

\item \textbf{Manipulator:} Controla el brazo robótico utilizado para manipular objetos en el entorno.

\item \textbf{Torso:} Permite el movimiento vertical del cuerpo del robot para alcanzar objetos a diferentes alturas.

\item \textbf{Mechatronic Head:} Controla los movimientos de la cabeza del robot, permitiendo orientar sensores como cámaras y micrófonos hacia puntos de interés.

\end{itemize}


\section{Arquitectura de un Sistema de Planeación de Acciones Jerárquico}

La planificación de acciones aborda el problema de generar secuencias de pasos u operaciones que permitan a un robot alcanzar objetivos de alto nivel en entornos parcialmente observables y dinámicos \cite{Russell2020}. Entre los paradigmas más consolidados para abordar este desafío se encuentra la planificación jerárquica, que descompone tareas complejas en subtareas más simples \cite{Nau2004}, Por ejemplo ("servir la cena") se descomponen en tareas intermedias ("ir a la cocina", "tomar platos", "colocar en mesa") y finalmente en acciones primitivas ejecutables por el robot. Este enfoque reduce significativamente la complejidad computacional del problema de planificación al explotar el conocimiento experto sobre cómo descomponer tareas, en contraste con los planificadores clásicos de primer principio que exploran espacios de estados sin guía estructural \cite{Erol1994}.

\subsection{Fundamentos de la Planificación Jerárquica: HTN}
El método más influyente de planificación jerárquica es el paradigma de \textbf{Redes de Tareas Jerárquicas} \textit{(Hierarchical Task Networks, HTN)} \cite{Georgievski2016}. En HTN, el problema de planificación no se define mediante un estado objetivo, sino mediante una tarea inicial (task network) que representa el objetivo de alto nivel a alcanzar \cite{Nau2004}. El conocimiento del dominio se codifica mediante dos elementos fundamentales:

\begin{itemize}

\item \textbf{Operadores:} Describen acciones primitivas ejecutables directamente por el robot, especificando sus precondiciones y efectos sobre el estado del mundo \cite{Fikes1971}. Son análogos a las acciones en \textbf{STRIPS} \textit{(Stanford Research Institute Problem Solver)}, el primer planificador clásico ampliamente adoptado \cite{Geffner2020}.

\item \textbf{Métodos :} Constituyen el núcleo de la jerarquía. Cada método especifica cómo descomponer una tarea compuesta (abstracta) en una red de subtareas, que pueden ser a su vez compuestas o primitivas \cite{Lallement2014}. Los métodos incluyen precondiciones que determinan cuándo es aplicable una determinada descomposición, permitiendo que el planificador elija entre múltiples formas de realizar una misma tarea según el contexto \cite{deSilva2016}.

\end{itemize}


El proceso de planificación HTN funciona mediante la descomposición recursiva: partiendo de la tarea inicial, el planificador selecciona métodos aplicables cuyas precondiciones se satisfacen en el estado actual, y reemplaza las tareas compuestas por sus subtareas, generando progresivamente una red completamente expandida en acciones primitivas \cite{Nau2003}. Cuando se alcanza una secuencia de acciones primitivas cuyas precondiciones pueden encadenarse consistentemente desde el estado inicial, se ha encontrado un plan solución.

Una ventaja distintiva de HTN es su capacidad para generar planes explicables y alineados con el conocimiento experto, ya que la jerarquía de métodos refleja cómo un diseñador humano concebiría la realización de tareas complejas \cite{Lallement2014}. 

\subsection{El Planificador HATP (Hierarchical Agent-Based Task Planner)}

Dentro de la familia de planificadores HTN orientados a robótica, destaca HATP (Hierarchical Agent-based Task Planner) , desarrollado en LAAS-CNRS para entornos de interacción humano-robot \cite{Lallement2014, deSilva2017}. HATP extiende el paradigma HTN tradicional con características específicamente diseñadas para robótica de servicio:

\begin{itemize}

\item \textbf{Tratamiento de agentes como entidades de primera clase:} HATP distingue explícitamente entre diferentes agentes (robots, humanos) en la representación del dominio, permitiendo asignar tareas a distintos ejecutores y generar planes multi-agente sincronizados \cite{Alami2013}.

\item \textbf{Reglas sociales (social rules):} Incorpora mecanismos para especificar restricciones sobre comportamientos aceptables en contextos de interacción humano-robot, como límites de tiempo de espera o secuencias de acciones que deben evitarse por razones de comodidad o seguridad \cite{Lallement2014}.

\item \textbf{Integración con razonamiento geométrico:} HATP puede intercalar planificación simbólica con validación geométrica mediante planificadores de movimiento (ej. Move3D), verificando en tiempo de planificación si las acciones consideradas son factibles en el mundo físico modelado con detalle 3D \cite{deSilva2017}. 
\end{itemize}

\subsection{Taxonomía de Arquitecturas Jerárquicas en Robótica}

Más allá de HTN, la literatura identifica diversas aproximaciones a la planificación jerárquica en robótica:

\begin{itemize}

\item \textbf{Planificación Jerárquica con Lógica Temporal (HTL):} Una línea reciente de investigación extiende la planificación jerárquica al dominio de especificaciones de lógica temporal proponiendo un framework de descomposición jerárquica basado en \textbf{LTL} \textit{(Linear Temporal Logic)} jerárquico \cite{Luo2024}, donde cada especificación de alto nivel se descompone en subtareas atómicas, infiriendo relaciones temporales entre ellas para construir una red de tareas. Este enfoque, validado experimentalmente en dominios de navegación y manipulación, permite manejar especificaciones complejas que resultarían inmanejables con una única fórmula LTL monolítica.

\item \textbf{Arquitecturas de Tres Capas (Three-Layer Architectures):} Un patrón arquitectónico ampliamente adoptado en robótica autónoma es la separación en capas deliberativa, de ejecución y reactiva \cite{Gat1998}. En el contexto de rehabilitación robótica, esta estructura se manifiesta como:

\begin{itemize}

\item \textbf{Capa física:} Maneja la interacción directa con el entorno mediante sensores y actuadores.

\item \textbf{Capa reactiva:} Ajusta en tiempo real parámetros de control (ej. asistencia, dificultad) basándose en retroalimentación sensorial.

\item \textbf{Capa deliberativa:} Utiliza planificadores como HTN o planificación basada en línea temporal para generar estrategias de intervención personalizadas a largo plazo \cite{Morales2025}.

\end{itemize}

\item \textbf{Planificación Jerárquica con Representaciones Ontológicas:} Investigaciones recientes exploran la integración de ontologías para enriquecer la representación del conocimiento en planificadores jerárquicos \cite{Pang2022}. Las ontologías permiten modelar relaciones semánticas entre objetos, agentes y tareas, facilitando el razonamiento sobre contextos no explícitamente programados y mejorando la adaptabilidad del planificador a situaciones novedosas.

\end{itemize}

\subsection{Relevancia para el Sistema Propuesto}

La arquitectura de planificación jerárquica presentada constituye el fundamento teórico sobre el cual se construye el sistema híbrido CLIPS-ChatGPT/Qwen propuesto en esta tesis. Específicamente:

\begin{itemize}

\item El motor CLIPS implementa un sistema de producción que, aunque no sigue estrictamente el formalismo HTN, puede organizarse jerárquicamente mediante reglas de prioridad y descomposición secuencial, como se detalla en la sección 4.1.1 \cite{Savage2024}.

\item La integración con ChatGPT/Qwen para planificación dual (sección 4.3) puede entenderse como una extensión de las arquitecturas híbridas que combinan planificadores simbólicos (como HATP) con módulos de razonamiento geométrico o semántico, pero sustituyendo estos últimos por modelos de lenguaje de gran escala con capacidad de generar planes contextualmente adaptativos \cite{Shi2023}.

\item La arquitectura ViRoot del robot Justina (Figura \ref{fig:viroot}) sigue explícitamente una organización jerárquica en capas de entradas, planificación, gestión de conocimiento y ejecución, alineándose con los principios de las arquitecturas de tres capas discutidas \cite{Savage2024}.

\end{itemize}


Esta base teórica proporciona el marco necesario para comprender cómo la combinación de razonamiento simbólico jerárquico y procesamiento flexible de lenguaje natural puede superar las limitaciones de ambos paradigmas por separado, constituyendo la hipótesis central de esta investigación.

\section{Fundamentos de los Sistemas de Producción (Rule-Based Systems)}

Los sistemas de producción, también conocidos como sistemas basados en reglas \textit{(Rule-Based Systems, RBS)}, constituyen uno de los paradigmas más establecidos en inteligencia artificial para la representación del conocimiento y el razonamiento automatizado \cite{MDX_Rete}. Su fundamento teórico reside en la representación del conocimiento mediante reglas del tipo if-then \textit{(condición-acción)}, que operan sobre una base de hechos que describe el estado actual del mundo \cite{Donnell1994}. En robótica, estos sistemas han demostrado particular utilidad para la planificación de acciones en entornos estructurados, donde la previsibilidad y la capacidad de explicación son requisitos fundamentales \cite{MDX_Rete}.

\subsection{Estructura y Componentes de un Sistema de Producción}

Un sistema de producción se compone de tres elementos fundamentales que interactúan de manera cíclica \cite{MDX_Rete}:

\begin{enumerate}

\item \textbf{Base de Hechos:}  Almacena la información relevante sobre el estado actual del entorno y del propio robot. Estos hechos representan afirmaciones atómicas sobre el mundo, como la ubicación de objetos, el estado de los actuadores o las percepciones sensoriales . En el contexto del robot Justina, la memoria de trabajo contendría hechos como \textit{(ubicacion robot cocina)} o \textit{(objeto vaso mesa)}.

\item \textbf{Base de Reglas:} Contiene el conjunto de reglas de producción que codifican el conocimiento del dominio. Cada regla presenta la forma general \cite{Donnell1994}:

$$
(condition 1) (condition 2) ... (condition m)
\Rightarrow (action 1) (action 2) ... (action n)
$$

La parte izquierda \textit{(Left-Hand Side, LHS)} especifica las condiciones que deben satisfacerse para que la regla sea aplicable, mientras que la parte derecha \textit{(Right-Hand Side, RHS)} define las acciones a ejecutar, que típicamente modifican la memoria de trabajo o producen efectos externos \cite{MDX_Rete}.

\item \textbf{Motor de Inferencia (Inference Engine):} Constituye el mecanismo de control que gobierna la aplicación de las reglas. Es responsable de determinar qué reglas son aplicables en cada momento, seleccionar una entre las posibles, y ejecutar sus acciones \cite{MDX_Rete}. El motor de inferencia implementa el ciclo fundamental de operación: el ciclo reconocer-actuar \textit{(recognize-act cycle)} \cite{Russell2020}.

\end{enumerate}

\subsection{El Ciclo Reconocer-Actuar (Recognize-Act Cycle)}

El ciclo reconocer-actuar constituye el corazón operativo de todo sistema basado en reglas. Este proceso iterativo se desarrolla en las siguientes fases \cite{Russell2020} \cite{MDX_Rete}:

\begin{enumerate}

\item \textbf{Reconocimiento:} El motor de inferencia compara el contenido actual de la memoria de trabajo con las condiciones de todas las reglas en la base de conocimiento. Aquellas reglas cuyas condiciones se satisfacen completamente con los hechos existentes se consideran \textit{"activadas"} y pasan a formar parte del conjunto de conflictos \cite{Russell2020}.

\item \textbf{Resolución de Conflictos:} Cuando múltiples reglas resultan aplicables simultáneamente, el motor debe seleccionar una para su ejecución. Esta decisión se realiza mediante estrategias de resolución de conflictos, que pueden basarse en prioridades explícitas de las reglas, especificidad de las condiciones, orden de activación, o políticas específicas del dominio \cite{MDX_Rete}.

\item \textbf{Actuación:} La regla seleccionada se ejecuta, realizando las acciones especificadas en su parte derecha. Estas acciones típicamente modifican la memoria de trabajo (añadiendo, eliminando o actualizando hechos) y pueden tener efectos secundarios sobre el entorno, y robot \cite{Russell2020} \cite{MDX_Rete}.

\item \textbf{Terminación:} El ciclo se repite hasta que se cumple un criterio de terminación, que puede ser la ausencia de reglas aplicables, la consecución del objetivo deseado, o una condición explícita de parada \cite{MDX_Rete}.


\end{enumerate}

\subsection{El Algoritmo RETE para Emparejamiento Eficiente de Patrones}

Una implementación ingenua del ciclo reconocer-actuar comprobaría cada regla contra todos los hechos en cada ciclo, resultando en una complejidad computacional prohibitiva incluso para sistemas de tamaño moderado \cite{Fanning1990}. Para abordar esta limitación, Charles Forgy desarrolló el \textbf{algoritmo RETE} durante su doctorado en \textit{Carnegie Mellon University (1979)}, cuya publicación apareció en Artificial Intelligence en 1982. El nombre "RETE" proviene del latín y significa "red", reflejando la estructura de red que constituye su esencia \cite{Fanning1990}.

\subsubsection*{Principios Fundamentales del Algoritmo RETE}

El algoritmo RETE optimiza el proceso de emparejamiento patrón-objeto mediante dos estrategias clave \cite{Fanning1990}:

\begin{enumerate}

\item \textbf{Almacenamiento de Estado:} RETE mantiene en memoria los resultados de emparejamientos previos, de modo que solo es necesario procesar los cambios incrementales en la memoria de trabajo, en lugar de reevaluar todas las reglas desde cero en cada ciclo.

\item \textbf{Compartición de subcálculos:} Cuando múltiples reglas comparten condiciones comunes, RETE evalúa dichas condiciones una sola vez y reutiliza los resultados para todas las reglas que las contienen.

\end{enumerate}

Estos principios se implementan mediante la construcción de una red de nodos (RETE network) que representa las condiciones de todas las reglas del sistema \cite{Fanning1990} \cite{Giarratano2005}.

\subsubsection*{Estructura de la Red RETE}

La red RETE se organiza jerárquicamente desde el nodo raíz hasta las hojas que representan reglas completas \cite{Fanning1990}:

\begin{itemize}

\item \textbf{Nodo Raíz:} Punto de entrada por el que fluyen todos los hechos introducidos en el sistema 	\cite{Giarratano2005}.

\item \textbf{Nodos Alfa:} Realizan pruebas simples sobre hechos individuales, como comprobaciones de tipo o igualdad de atributos. Cada nodo alfa corresponde a un patrón atómico dentro de las condiciones de las reglas \cite{UNIZA_RBS}. Estos nodos operan de manera independiente y almacenan en su memoria los hechos que satisfacen su prueba \cite{Fanning1990}.

\item \textbf{Nodos Beta:} Implementan operaciones de unión entre conjuntos de hechos procedentes de diferentes caminos en la red. Cada nodo beta combina los resultados de dos nodos anteriores (típicamente uno alfa y otro beta) verificando que las variables compartidas tengan vinculaciones consistentes \cite{ScienceDirect_Production} \cite{UNIZA_RBS}. La memoria de los nodos beta almacena los pares o tuplas que han superado la condición de unión \cite{Fanning1990}.

\item \textbf{Nodos Terminales:} Representan reglas completas. Cuando un token alcanza un nodo terminal, significa que se ha encontrado una combinación de hechos que satisface todas las condiciones de la regla correspondiente, quedando esta activada para su ejecución \cite{Fanning1990} \cite{Giarratano2005}.

\end{itemize}

\subsubsection*{Funcionamiento Incremental}

El proceso de evaluación en RETE es incremental . Cuando se añade, modifica o elimina un hecho en la memoria de trabajo:

\begin{enumerate}

\item El hecho ingresa por el nodo raíz y se propaga hacia abajo a través de los nodos alfa cuyas pruebas supera \cite{Fanning1990}.

\item Los nodos alfa actualizan sus memorias y propagan los cambios a los nodos beta conectados.

\item Los nodos beta evalúan las nuevas combinaciones posibles con los tokens almacenados en sus memorias izquierda y derecha, actualizando sus propias memorias y propagando los resultados \cite{ScienceDirect_Production} \cite{Giarratano2005}.

\item Finalmente, los cambios alcanzan los nodos terminales, actualizando dinámicamente el conjunto de reglas activadas \cite{Fanning1990}.

\end{enumerate}

Este enfoque incremental reduce drásticamente el trabajo computacional, ya que solo se procesan los cambios efectivos en lugar de reevaluar todo el sistema en cada ciclo. La complejidad temporal del algoritmo RETE es, en promedio, independiente del número total de reglas en el sistema, dependiendo principalmente del número de hechos y de la estructura de las condiciones \cite{Forgy1982}.

\subsection{Implementación de RETE en CLIPS}

CLIPS implementa una versión optimizada del algoritmo RETE adaptada a sus necesidades específicas como sistema de producción de propósito general .

\subsubsection*{Estructura de Memoria en CLIPS}
CLIPS mantiene dos tipos principales de memoria :

\begin{itemize}

\item \textbf{Memoria de Trabajo:} Almacena los hechos que representan el estado actual del sistema. Cada hecho puede ser simple (ordenado) o estructurado mediante plantillas (deftemplate).

\item \textbf{Memoria de Hechos:} Una estructura interna que permite el acceso eficiente a los hechos durante el proceso de emparejamiento.

\end{itemize}


\subsubsection*{El Motor de Inferencia de CLIPS}

El motor de inferencia de CLIPS implementa el ciclo reconocer-actuar completo, utilizando la red RETE para la fase de reconocimiento \cite{Forgy1979}:

\begin{enumerate}

\item \textbf{Fase de Reconocimiento:} Los cambios en la memoria de trabajo se propagan a través de la red RETE precompilada a partir de las reglas del sistema. La red mantiene información sobre qué condiciones se satisfacen parcial o completamente.

\item \textbf{Fase de Resolución de Conflictos:} CLIPS ofrece múltiples estrategias de resolución de conflictos, incluyendo prioridad (salience), profundidad (depth-first), amplitud (breadth-first), y estrategias basadas en la especificidad de las reglas 	\cite{Forgy1979}.

\item \textbf{Fase de Actuación:} La regla seleccionada ejecuta sus acciones, que pueden incluir la modificación de la memoria de trabajo, la ejecución de funciones externas, o la interacción con el entorno a través de la API de CLIPS.

\end{enumerate}



\subsubsection*{Funciones Objeto en CLIPS}
 
A partir de CLIPS 6.0, el sistema incorpora capacidades de programación orientada a objetos mediante el módulo COOL (CLIPS Object-Oriented Language) 	\cite{Forgy1979}. Esta extensión permite:

\begin{itemize}

\item Definir clases con atributos y métodos.

\item Crear instancias de objetos que residen en una memoria de objetos separada.

\item Realizar emparejamiento de patrones sobre objetos mediante la función \textit{object-pattern-match}.

\end{itemize}


La implementación del algoritmo RETE en CLIPS 6.0 se extendió para soportar el emparejamiento sobre objetos, manteniendo la eficiencia del algoritmo original pero permitiendo representaciones de conocimiento más ricas y estructuradas. Los objetos se tratan como hechos especiales que pueden ser referenciados desde las condiciones de las reglas, y la red RETE maneja tanto hechos tradicionales como objetos de manera unificada \cite{Forgy1979}.

\subsubsection*{Consideraciones de Rendimiento}

Estudios sobre implementaciones de CLIPS en entornos empotrados han identificado cinco parámetros clave que afectan el rendimiento del motor de inferencia :

\begin{enumerate}

\item El número de objetos en la memoria de trabajo.

\item La complejidad estructural de los objetos y las reglas.

\item El número de reglas que comparten patrones de emparejamiento comunes.

\item El número de patrones por regla.

\item El número de objetos vinculados a cada patrón.

\end{enumerate}

En aplicaciones de tiempo real el tiempo de respuesta del motor de inferencia puede ser adecuado, aunque los requisitos de memoria pueden ser significativos dependiendo de la complejidad de la red RETE \cite{Murphy2019}. Esta característica hace que CLIPS sea particularmente adecuado para plataformas robóticas con restricciones computacionales moderadas.


\subsection{Ventajas y Limitaciones de los Sistemas de Producción}

Los sistemas basados en reglas presentan ventajas significativas que los hacen útiles en robótica 	\cite{MDX_Rete}:

\begin{itemize}

\item \textbf{Ventajas:}
	
\begin{itemize}
	
	\item \textbf{Modularidad:} Las reglas pueden añadirse, modificarse o eliminarse independientemente, facilitando el mantenimiento y la 	evolución del sistema.

	\item \textbf{Explicabilidad:} El razonamiento puede rastrearse y explicarse mediante la inspección de las reglas activadas, lo que es crucial para la auditoría y la confianza en sistemas críticos.

	\item \textbf{Naturalidad:} Las reglas expresadas en formato if-then se aproximan a la forma en que los humanos describen procedimientos y conocimientos.

	\item \textbf{Separación conocimiento-control:} El conocimiento del dominio (reglas) está claramente separado del mecanismo de control (motor de inferencia), permitiendo modificaciones sin afectar la arquitectura subyacente.
	
\end{itemize}
	
\item \textbf{Limitaciones:}

\begin{itemize}

\item \textbf{Dificultad de análisis:} El flujo de control puede ser difícil de predecir cuando múltiples reglas interactúan de maneras complejas.

\item \textbf{Ausencia de aprendizaje:} Los sistemas clásicos de producción no incorporan capacidades de aprendizaje automático, requiriendo que todo el conocimiento sea explícitamente programado.

\item \textbf{Ineficiencia potencial:} Aunque RETE optimiza el emparejamiento, sistemas con miles de reglas pueden presentar problemas de rendimiento.

\item \textbf{Gestión de conflictos:} La resolución de conflictos requiere diseños cuidadosos para evitar comportamientos indeseados.

\end{itemize}

\end{itemize}

Estas limitaciones, particularmente la rigidez semántica y la ausencia de aprendizaje, motivan la integración con modelos de lenguaje grande propuesta en esta tesis, buscando combinar la robustez y transparencia de los sistemas basados en reglas con la flexibilidad y capacidad adaptativa de los enfoques subsimbólicos modernos \cite{MDX_Rete}.

\section{Introducción a los Modelos de Lenguaje Grande (LLMs) y ChatGPT/Qwen}

Los Modelos de Lenguaje Grande (Large Language Models, LLMs) han emergido como una de las tecnologías más transformadoras en el campo de la inteligencia artificial, revolucionando la manera en que las máquinas procesan y generan lenguaje natural \cite{Shi2023}. Su aplicación en robótica de servicio abre nuevas posibilidades para la interacción humano-robot, permitiendo que usuarios no expertos comuniquen sus intenciones mediante comandos verbales complejos sin necesidad de conocer lenguajes de programación estructurados \cite{Zeng2020}. Esta sección expone los fundamentos arquitectónicos de los LLMs, centrándose en el mecanismo de atención y la arquitectura Transformer, para posteriormente contrastar dos modelos representativos: ChatGPT (modelo propietario) y Qwen2.5-0.5B (modelo de código abierto), evaluando su idoneidad para entornos robóticos con recursos computacionales limitados.

\subsection{Fundamentos Arquitectónicos: Transformers y Mecanismo de Atención}

\subsubsection*{Limitaciones de los Modelos Secuenciales Previos}

Antes de la introducción de la arquitectura Transformer en 2017, el procesamiento de lenguaje natural estaba dominado por redes neuronales recurrentes (RNN) y sus variantes, como LSTM (Long Short-Term Memory) y GRU (Gated Recurrent Units) \cite{Russell2020}. Estos modelos procesan el texto de manera secuencial, token por token, lo que presenta dos limitaciones fundamentales \cite{Vaswani2017}:

\begin{enumerate}

\item \textbf{Dificultad para el paralelismo:} La naturaleza secuencial de las RNN impide el aprovechamiento eficiente de hardware paralelo como GPUs, resultando en tiempos de entrenamiento prolongados \cite{UNIZA_RBS}.

\item \textbf{Problema de dependencias de largo alcance:} Aunque LSTM y GRU mitigan parcialmente el problema del gradiente evanescente, la información sobre palabras distantes tiende a diluirse a medida que la secuencia se alarga, dificultando el modelado de relaciones semánticas entre elementos lejanos \cite{Albert1989}.

\end{enumerate}

\subsubsection*{La Arquitectura Transformer}

El artículo seminal "Attention Is All You Need", publicado por investigadores de Google en 2017, introdujo la arquitectura Transformer, que prescinde por completo de recurrencia y convoluciones, basándose únicamente en mecanismos de atención \cite{Vaswani2017}. Esta arquitectura se ha convertido en el fundamento de todos los LLMs modernos, incluyendo GPT, BERT, y sus derivados \cite{Shi2023}.

La arquitectura Transformer sigue una estructura codificador-decodificador (encoder-decoder) con las siguientes características principales \cite{Vaswani2017}:

\begin{itemize}

\item \textbf{Codificador (Encoder):} Compuesto por una pila de $N=6$ capas idénticas. Cada capa contiene dos subcapas: un mecanismo de autoatención multi-cabeza (multi-head self-attention) y una red feed-forward posicional totalmente conectada. Alrededor de cada subcapa se emplean conexiones residuales seguidas de normalización por capas (layer normalization).

\item \textbf{Decodificador (Decoder):} Similar al codificador, pero con una tercera subcapa que realiza atención cruzada (cross-attention) sobre la salida del codificador. Además, incorpora un enmascaramiento para evitar que las posiciones futuras influyan en la predicción actual, preservando la propiedad autorregresiva del modelo.

\end{itemize}

\subsubsection*{El Mecanismo de Atención}


\section{Integración de Traductores de Lenguaje Natural con Sistemas Basados en Reglas}
\subsection{Utilidad y Función de la Integración}
\subsection{Funcionamiento de la Integración CLIPS-Lenguaje Natural}
\subsection{Descripción de Tecnologías: CLIPS, ChatGPT y Qwen2.5-0.5B}
\section{ChatGPT como Agente de Planificación Autónomo}
\subsection{Capacidades de Planificación de LLMs vs. Sistemas Basados en Reglas}

Análisis comparativo de los enfoques de planificación, destacando la rigidez predecible de CLIPS frente a la adaptabilidad contextual de los LLMs.

\subsection{Arquitectura de Planificación Dual: CLIPS y ChatGPT como Motores Complementarios}
\subsection{Mecanismos de Selección y Validación de Planes}
\subsection{Ventajas y Riesgos de la Planificación con LLMs en Robótica}