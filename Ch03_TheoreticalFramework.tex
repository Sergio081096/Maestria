\chapter{Formalización del problema y arquitectura conceptual}
\label{ch:formalizacion}

El problema abordado en esta tesis consiste en transformar una instrucción
expresada en lenguaje natural en una secuencia de acciones que pueda ser
procesada por el sistema de ejecución de un robot de servicio. Esta
transformación requiere relacionar dos niveles de representación diferentes:
por una parte, la intención expresada por el usuario y, por otra, las acciones
concretas disponibles en el robot.

Como se revisó en el capítulo anterior, los modelos de lenguaje proporcionan
mecanismos para procesar la variabilidad presente en las instrucciones
lingüísticas, mientras que los sistemas simbólicos permiten representar
conocimiento y relaciones mediante estructuras explícitas. A partir de estos
antecedentes, en este capítulo se formaliza el problema particular considerado
en la investigación y se establecen las representaciones utilizadas para
describir órdenes, objetivos y planes.

El dominio experimental toma como referencia las tareas de General Purpose
Service Robot (GPSR) de RoboCup@Home. Sin embargo, GPSR se utiliza como una
fuente controlada de tareas, entidades y escenarios, y no como una restricción
que obligue al usuario a reproducir literalmente las formulaciones
lingüísticas utilizadas en la competencia.

Finalmente, se presentan las dos arquitecturas que serán evaluadas. La primera
utiliza Qwen2.5-7B como intérprete semántico y CLIPS como componente encargado
de expandir los objetivos obtenidos en acciones ejecutables. La segunda delega
tanto la interpretación como la generación del plan a un modelo GPT. Ambas
arquitecturas utilizan el mismo dominio conceptual, pero distribuyen de manera
diferente la responsabilidad de transformar una orden en un plan.


% ============================================================
\section{Contexto arquitectónico y alcance del sistema}
\label{sec:contexto_arquitectonico}

\subsection{Integración dentro de la arquitectura ViRBot}
\label{subsec:integracion_virbot}

La investigación se desarrolla sobre el robot de servicio Justina y su
arquitectura de software ViRBot. Esta arquitectura integra diferentes
capacidades necesarias para la operación del robot, entre ellas percepción,
interacción humano--robot, navegación, manipulación, representación de
conocimiento, planificación y ejecución.

% TODO: revisar referencia correspondiente a ViRBot y procedencia de la figura.

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.8\textwidth]{Figures/virbot.png}
    \caption{Arquitectura general ViRBot del robot Justina.}
    \label{fig:virbot}
\end{figure}

El sistema desarrollado en esta tesis se ubica entre la recepción de una
instrucción expresada en lenguaje natural y los componentes encargados de
ejecutar las acciones correspondientes. Por tanto, no pretende sustituir los
módulos de navegación, percepción o manipulación del robot, sino proporcionar
una representación ordenada de las acciones que dichos módulos deben realizar.

De manera simplificada, el flujo general puede expresarse como:

\begin{equation}
\boxed{
\text{orden en lenguaje natural}
\rightarrow
\text{interpretación y planificación}
\rightarrow
\text{plan}
\rightarrow
\text{ejecución}
}
\end{equation}

La contribución estudiada se concentra principalmente en las transformaciones
anteriores a la ejecución física. Los módulos existentes de Justina son los
responsables de intentar realizar las acciones especificadas en el plan.


\subsection{Alcance del problema abordado}
\label{subsec:alcance_problema}

Una instrucción proporcionada por una persona puede contener diferentes
subtareas relacionadas entre sí. Por ejemplo:

\begin{quote}
\textit{``Go to the kitchen, find an apple and bring it to me.''}
\end{quote}

Esta instrucción contiene información relacionada con navegación, búsqueda,
manipulación y entrega. Para que pueda ser procesada por el robot es necesario
identificar dichas intenciones y convertirlas posteriormente en operaciones
compatibles con sus capacidades.

El problema estudiado puede expresarse inicialmente mediante la transformación:

\begin{equation}
x \rightarrow P,
\end{equation}

donde $x$ representa una orden expresada en lenguaje natural y $P$ el plan
generado para atenderla.

Sin embargo, esta representación oculta la diferencia entre comprender la
orden y determinar las acciones necesarias para realizarla. Para analizar estas
dos funciones por separado, en esta tesis se introduce una representación
semántica intermedia $G$:

\begin{equation}
\boxed{
x \rightarrow G \rightarrow P
}
\label{eq:problema_general}
\end{equation}

donde:

\begin{itemize}
    \item $x$ es la orden original;
    \item $G$ es una secuencia estructurada de objetivos semánticos;
    \item $P$ es una secuencia de acciones compatibles con el robot.
\end{itemize}

Esta separación constituye la base conceptual utilizada posteriormente para
comparar las arquitecturas Qwen--CLIPS y GPT directo.


% ============================================================
\section{GPSR como dominio de referencia}
\label{sec:gpsr_referencia}

\subsection{La prueba General Purpose Service Robot}
\label{subsec:gpsr}

General Purpose Service Robot (GPSR) es una prueba de RoboCup@Home orientada a
evaluar la capacidad de un robot de servicio para comprender instrucciones
expresadas en lenguaje natural y ejecutar tareas que pueden requerir la
combinación de distintas habilidades. Entre ellas se encuentran la navegación,
la búsqueda y reconocimiento de personas u objetos, la manipulación y la
interacción humano--robot\cite{robocuphome2026rulebook}.

Una característica importante de GPSR es que una instrucción puede representar
una tarea compuesta. Por ejemplo, una orden puede requerir que el robot se
desplace hacia una habitación, localice un objeto, lo tome y posteriormente lo
entregue a una persona. En consecuencia, el problema no consiste únicamente en
reconocer palabras o entidades aisladas, sino en identificar las diferentes
intenciones contenidas en la instrucción y conservar las relaciones existentes
entre ellas.

En la prueba considerada en esta investigación se proporcionan tres comandos al
robot. Al comenzar, el robot se desplaza hacia un \textit{Instruction Point},
que funciona como el punto de interacción con el operador. Desde esta posición
recibe una instrucción y, una vez que ha realizado suficientemente la tarea
solicitada, regresa al mismo punto para recibir el siguiente comando. La
determinación de que una tarea ha sido completada de manera suficiente para
continuar con la prueba corresponde al árbitro.

La entrega de las órdenes se realiza de manera secuencial. Por tanto, el robot
no recibe un nuevo comando mientras la ejecución de la tarea anterior continúe
en curso. Una vez concluida la tarea y después de regresar al
\textit{Instruction Point}, puede solicitar la siguiente instrucción hasta
completar los tres comandos contemplados por la prueba.

Cuando el robot no logra comprender adecuadamente la instrucción recibida
mediante su sistema de entrada de audio, puede solicitar explícitamente que la orden sea reformulada. En este caso, el árbitro u operador puede expresar la
misma tarea mediante una construcción lingüística más sencilla, pero debe
mantener los objetivos contenidos en la instrucción original. La reformulación
no constituye una modificación de la tarea ni permite que el operador indique
directamente al robot cómo resolverla.

Este mecanismo es particularmente relevante para la presente investigación,
pues permite distinguir entre la tarea que debe realizarse y la forma
lingüística utilizada para comunicarla. Dos instrucciones con estructuras
sintácticas diferentes pueden representar los mismos objetivos y, por tanto,
deberían conducir a una interpretación semántica equivalente.

Durante la ejecución no se contempla que una persona intervenga para
descomponer la orden, seleccionar las acciones del robot o corregir
manualmente el plan generado. La interpretación de la instrucción y la
determinación de las acciones necesarias corresponden al propio sistema
robótico. De esta manera, GPSR proporciona un escenario apropiado para evaluar
la transformación de instrucciones en lenguaje natural en representaciones
semánticas y planes de acción.

\subsection{Generación de comandos y conocimiento del dominio}
\label{subsec:gpsr_generador}

Una característica relevante de GPSR es que los comandos utilizados durante la
prueba no se seleccionan únicamente a partir de una lista fija de oraciones.
El reglamento establece que las tareas se construyen mediante el
\textit{CommandGenerator} oficial de RoboCup@Home
\cite{robocuphome2026rulebook,robocup_command_generator}. Sobre esta herramienta se realizaron posteriormente adaptaciones específicas para construir el conjunto de datos empleado en el entrenamiento y evaluación del modelo. Dichas modificaciones se
describen en el capítulo de metodología.

El generador define diferentes familias de instrucciones relacionadas con
personas y objetos. Entre ellas se encuentran tareas para desplazarse hacia una
ubicación, encontrar personas u objetos, contar entidades, obtener información,
seguir o guiar personas, tomar objetos y entregarlos o colocarlos en una
ubicación determinada. Algunas de estas estructuras permiten incorporar
continuaciones que producen comandos compuestos. De esta manera, una
instrucción inicial puede extenderse con subtareas posteriores que dependen del
resultado de las anteriores \cite{robocup_command_generator}.

De forma simplificada, el proceso puede representarse como:

\begin{equation}
\boxed{
\text{familia de tarea}
+
\text{estructura lingüística}
+
\text{entidades del dominio}
\rightarrow
\text{comando GPSR}
}
\end{equation}

Las estructuras del generador utilizan elementos variables para representar
entidades y relaciones del dominio. Por ejemplo, una plantilla puede contener
una habitación, una persona, un objeto o una ubicación que posteriormente son
sustituidos por valores concretos. De forma similar, diferentes verbos pueden
representar una intención relacionada; por ejemplo, una tarea de navegación
puede expresarse mediante verbos como \textit{go} o \textit{navigate}, mientras
que una búsqueda puede utilizar expresiones como \textit{find},
\textit{locate} o \textit{look for}
\cite{robocup_command_generator}.

El conocimiento utilizado para realizar estas sustituciones se mantiene
separado de las estructuras de los comandos. El generador distingue, entre
otros elementos, nombres de personas, habitaciones, ubicaciones, superficies
de colocación, objetos y categorías de objetos
\cite{robocup_command_generator}. La herramienta permite recibir este
conocimiento desde un directorio externo y señala el repositorio
\textit{CompetitionTemplate} como referencia para su organización
\cite{robocup_command_generator,robocup_competition_template}.

En \textit{CompetitionTemplate}, la información específica de una competencia
se organiza en archivos relacionados con el mapa y las ubicaciones, los nombres
de personas y los objetos disponibles
\cite{robocup_competition_template}. Esta separación resulta importante porque
permite mantener las estructuras generales de las tareas mientras se modifican
las entidades concretas presentes en una edición o escenario determinado.

Conceptualmente, puede representarse el dominio de referencia como:

\begin{equation}
D_{\mathrm{GPSR}}=(F,K),
\end{equation}

donde $F$ representa las familias de tareas contempladas por el generador y $K$
el conocimiento específico utilizado para instanciarlas. La estructura
detallada de $K$ y su utilización en esta investigación se formalizan en la
Sección~\ref{subsec:entidades_dominio}.


\subsection{Dominio de referencia y alcance lingüístico}
\label{subsec:dominio_referencia}

La utilización de GPSR como referencia no implica que el sistema desarrollado
en esta tesis deba reconocer únicamente las frases producidas literalmente por
el generador oficial. Esta distinción resulta especialmente importante debido
a que una misma intención puede expresarse mediante diferentes construcciones
lingüísticas.

El propio procedimiento de GPSR 2026 introduce variación sobre las órdenes
generadas. Una vez producido un comando mediante el generador oficial, el
reglamento establece que éste se proporciona a un modelo de lenguaje para
obtener una formulación similar. Además, una instrucción puede ser reformulada
hasta tres veces, utilizando expresiones progresivamente más simples cuando sea
necesario \cite{robocuphome2026rulebook}.

Por ejemplo, el reglamento presenta una transformación equivalente a pasar de
una instrucción como:

\begin{quote}
\textit{``Get me a coke from the kitchen.''}
\end{quote}

a una formulación que explicita sus subtareas, como:

\begin{quote}
\textit{``Go to the kitchen, find a coke, and bring it to me.''}
\end{quote}

Aunque la estructura superficial de ambas expresiones es diferente, la tarea
subyacente permanece esencialmente sin cambios. Esto permite distinguir dos
niveles:

\begin{equation}
\text{forma lingüística}
\qquad\text{y}\qquad
\text{intención semántica}.
\end{equation}

Esta distinción es central para la presente investigación. GPSR proporciona un
conjunto controlado de tareas y entidades, pero la entrada lingüística puede
presentar diferentes formas de expresar una misma intención. El objetivo del
intérprete no consiste, por tanto, en memorizar las plantillas del generador,
sino en establecer una correspondencia entre distintas realizaciones
lingüísticas y una representación semántica común.

Por ejemplo, considérese:

\begin{quote}
\textit{``Go to the kitchen and find Robin.''}
\end{quote}

y:

\begin{quote}
\textit{``Could you look for Robin in the kitchen?''}
\end{quote}

Dentro del dominio considerado, ambas pueden corresponder a la misma secuencia
de objetivos:

\begin{verbatim}
go(kitchen)
find(Robin, kind=person)
\end{verbatim}

Por tanto, en esta tesis GPSR se utiliza como una fuente controlada para definir
las tareas, entidades y relaciones que forman parte del dominio experimental,
pero no como una gramática que limite las únicas formulaciones aceptables por
el sistema.

Puede resumirse esta relación mediante:

\begin{equation}
\boxed{
\begin{array}{c}
\text{múltiples formulaciones lingüísticas}
\\
\downarrow
\\
\text{representación semántica canónica}
\\
\downarrow
\\
\text{capacidades implementadas del robot}
\end{array}
}
\end{equation}

En consecuencia, la entrada lingüística puede variar en su formulación, pero
la interpretación permanece acotada por el dominio semántico y las capacidades
operativas disponibles. Una instrucción puede ser perfectamente comprensible
para una persona y, aun así, quedar fuera del alcance del sistema si requiere
una intención que no puede representarse mediante los objetivos contemplados o
una capacidad que el robot no posee.

El dominio experimental de esta investigación considera principalmente
instrucciones en inglés, en correspondencia con las estructuras utilizadas por
el generador y con el escenario de evaluación. Por ello, los resultados
relacionados con variabilidad lingüística deben interpretarse como capacidad
para procesar diferentes formulaciones de las tareas soportadas dentro de dicho
idioma, y no como evidencia de independencia respecto al idioma o de
comprensión irrestricta del lenguaje natural.

% ============================================================
\section{Formalización de órdenes, objetivos y planes}
\label{sec:formalizacion_ordenes}

La transformación de una instrucción en lenguaje natural a un plan para un
robot de servicio implica relacionar representaciones con diferentes niveles
de abstracción. La expresión proporcionada por el usuario describe una tarea
mediante lenguaje natural, mientras que el sistema de ejecución requiere una
secuencia de acciones pertenecientes a un repertorio previamente definido.

En esta investigación se introduce una representación semántica intermedia que
permite separar ambos problemas. De manera general, el proceso se representa
como:

\begin{equation}
\boxed{
x \rightarrow G \rightarrow P
}
\label{eq:orden_goals_plan}
\end{equation}

donde $x$ representa la instrucción expresada en lenguaje natural, $G$ una
secuencia ordenada de objetivos semánticos y $P$ el plan compuesto por acciones
compatibles con las capacidades del robot.

Esta separación permite analizar independientemente dos problemas. El primero
consiste en determinar qué solicita el usuario:

\begin{equation}
x \rightarrow G,
\end{equation}

mientras que el segundo consiste en determinar qué acciones son necesarias
para realizar los objetivos obtenidos:

\begin{equation}
G \rightarrow P.
\end{equation}

Las siguientes subsecciones formalizan las representaciones utilizadas para
describir estas transformaciones.


% ------------------------------------------------------------
\subsection{Orden en lenguaje natural}
\label{subsec:orden_natural}

Sea:

\begin{equation}
x \in \mathcal{X}
\end{equation}

una instrucción expresada en lenguaje natural perteneciente al conjunto
$\mathcal{X}$ de órdenes consideradas dentro del dominio experimental.

Una instrucción puede describir una tarea simple o una composición de varias
subtareas relacionadas. Por ejemplo:

\begin{quote}
\textit{``Go to the kitchen, find an apple and bring it to me.''}
\end{quote}

contiene información relacionada con navegación, búsqueda, manipulación y
entrega de un objeto.

Por tanto, una instrucción lingüística no corresponde necesariamente con una
única acción del robot:

\begin{equation}
\boxed{
\text{una instrucción}
\not\equiv
\text{una acción}
}
\end{equation}

Incluso una expresión lingüísticamente breve puede implicar varias operaciones.
Por ejemplo:

\begin{quote}
\textit{``Bring me an apple from the kitchen.''}
\end{quote}

puede representarse mediante la siguiente secuencia semántica:

\begin{verbatim}
go(kitchen)
find(apple, kind=object)
take(apple)
deliver(apple, to=me)
\end{verbatim}

En consecuencia, el objetivo de la interpretación no consiste únicamente en
identificar palabras clave, sino en determinar las subtareas implícitas o
explícitas en la instrucción, las entidades involucradas y las relaciones
existentes entre ellas.

Formalmente, la interpretación puede expresarse mediante una función:

\begin{equation}
I:
\mathcal{X}
\rightarrow
\mathcal{G}^{*},
\end{equation}

donde $\mathcal{G}^{*}$ representa el conjunto de secuencias finitas que pueden
formarse a partir del vocabulario de objetivos semánticos $\mathcal{G}$.

Para una instrucción particular $x$, la función produce:

\begin{equation}
I(x)=G=(g_1,g_2,\ldots,g_n).
\end{equation}

El número $n$ de objetivos obtenidos depende de la tarea representada por la
instrucción y no necesariamente de su longitud superficial.


% ------------------------------------------------------------
\subsection{Equivalencia semántica entre formulaciones}
\label{subsec:equivalencia_semantica}

Como se describió en la Sección~\ref{sec:gpsr_referencia}, una misma tarea puede
ser expresada mediante diferentes formulaciones lingüísticas. Esta propiedad
es particularmente relevante en GPSR, donde una orden puede ser reformulada
manteniendo los objetivos de la instrucción original.

Sean dos instrucciones:

\begin{equation}
x_1,x_2\in\mathcal{X}.
\end{equation}

En el contexto de esta investigación, se consideran semánticamente
equivalentes cuando ambas representan la misma secuencia canónica de
objetivos. Esta relación se denota mediante:

\begin{equation}
x_1 \sim x_2
\end{equation}

si:

\begin{equation}
I(x_1)=I(x_2)=G.
\label{eq:equivalencia_semantica}
\end{equation}

Por ejemplo, las instrucciones:

\begin{quote}
\textit{``Bring me an apple from the kitchen.''}
\end{quote}

y:

\begin{quote}
\textit{``Go to the kitchen, find an apple, take it and bring it to me.''}
\end{quote}

presentan estructuras lingüísticas diferentes, pero pueden corresponder a la
misma secuencia semántica:

\begin{verbatim}
go(kitchen)
find(apple, kind=object)
take(apple)
deliver(apple, to=me)
\end{verbatim}

Por tanto, ambas expresiones pertenecen conceptualmente a una misma clase de
equivalencia:

\begin{equation}
[x]
=
\{
x'\in\mathcal{X}
\mid
I(x')=I(x)
\}.
\end{equation}

Esta representación permite expresar uno de los objetivos principales de la
etapa de interpretación: diferentes realizaciones superficiales de una misma
tarea deben converger hacia una representación semántica común.

De manera esquemática:

\begin{equation}
\boxed{
\begin{array}{ccc}
x_1 & & \\
x_2 & \longrightarrow & G \\
\vdots & & \\
x_k & &
\end{array}
}
\end{equation}

donde $x_1,\ldots,x_k$ representan distintas formas de expresar una intención
equivalente.

Esta propiedad permite separar la variabilidad lingüística del dominio
semántico. El sistema puede admitir diferentes formas de formular una tarea sin
que ello implique incrementar indefinidamente el conjunto de objetivos que debe
ser capaz de representar.

En consecuencia, el problema de interpretación puede entenderse como una
transformación desde un espacio lingüístico de alta variabilidad hacia un
espacio semántico más reducido y estructurado:

\begin{equation}
\boxed{
\mathcal{X}
\xrightarrow{I}
\mathcal{G}^{*}
}
\end{equation}

El objetivo no es memorizar las estructuras superficiales utilizadas para
generar los comandos de referencia, sino identificar la intención que dichas
estructuras representan.


% ------------------------------------------------------------
\subsection{Entidades y conocimiento del dominio}
\label{subsec:entidades_dominio}

La interpretación de una instrucción requiere disponer de información sobre
las entidades que pueden participar en las tareas consideradas.

En esta investigación, el conocimiento relevante del dominio se representa
como:

\begin{equation}
K=
\{
K_P,
K_O,
K_C,
K_L,
K_R
\},
\label{eq:conocimiento_dominio}
\end{equation}

donde:

\begin{itemize}

    \item $K_P$ representa el conjunto de personas o nombres conocidos;

    \item $K_O$ representa el conjunto de objetos;

    \item $K_C$ representa las categorías de objetos;

    \item $K_L$ representa las ubicaciones específicas;

    \item $K_R$ representa las habitaciones.

\end{itemize}

Cada uno de estos subconjuntos puede expresarse, de manera general, como:

\begin{equation}
K_P=\{p_1,p_2,\ldots,p_{n_P}\},
\end{equation}

\begin{equation}
K_O=\{o_1,o_2,\ldots,o_{n_O}\},
\end{equation}

y de forma equivalente para los demás componentes del dominio.

Esta información permite determinar el tipo semántico de diferentes
referencias. Por ejemplo:

\begin{equation}
Robin\in K_P,
\end{equation}

mientras que:

\begin{equation}
apple\in K_O.
\end{equation}

Por tanto:

\begin{verbatim}
find(Robin, kind=person)
\end{verbatim}

y:

\begin{verbatim}
find(apple, kind=object)
\end{verbatim}

comparten el mismo tipo general de objetivo, pero operan sobre entidades
semánticamente diferentes.

El conocimiento del dominio cumple al menos dos funciones. En primer lugar,
permite restringir las entidades nominales que pueden ser consideradas
válidas. Una salida que introduzca arbitrariamente una persona, objeto o
ubicación que no pertenece al dominio no debe interpretarse automáticamente
como correcta.

En segundo lugar, proporciona información para resolver el tipo de las
entidades y verificar la compatibilidad entre ellas y los objetivos en los que
participan.

Las referencias presentes en una instrucción pueden adoptar distintas formas.
En esta investigación se distinguen, de manera conceptual, tres casos
principales:

\begin{enumerate}

    \item \textbf{Entidades nominales:} referencias explícitas a una entidad
    conocida, como \texttt{Robin}, \texttt{apple} o \texttt{kitchen}.

    \item \textbf{Entidades genéricas:} referencias a una clase de entidad,
    como \texttt{person}, \texttt{drink} o \texttt{fruit}.

    \item \textbf{Entidades descritas mediante propiedades:} referencias que
    requieren identificar una entidad a partir de alguna característica, como
    una persona saludando o el objeto de mayor tamaño.

\end{enumerate}

Por ejemplo:

\begin{verbatim}
find(person, kind=person, gesture='waving person')
\end{verbatim}

no identifica inicialmente a una persona mediante un nombre, sino mediante una
propiedad observable.

De manera similar:

\begin{verbatim}
find(drink, kind=object, property=largest)
\end{verbatim}

representa la búsqueda de un objeto perteneciente a una categoría y sujeto a
una propiedad adicional.

Esta distinción permite conservar información semántica que posteriormente
puede ser utilizada por los componentes de percepción y planificación.


% ------------------------------------------------------------
\subsection{Representación canónica de objetivos}
\label{subsec:objetivos_canonicos}

El resultado de la interpretación se representa mediante una secuencia
ordenada:

\begin{equation}
G=(g_1,g_2,\ldots,g_n),
\label{eq:secuencia_goals}
\end{equation}

donde cada $g_i$ representa un objetivo semántico.

De manera general:

\begin{equation}
g_i=(t_i,\theta_i),
\end{equation}

donde $t_i$ identifica el tipo de objetivo y $\theta_i$ contiene los
argumentos y calificadores necesarios para describirlo.

Equivalentemente, puede utilizarse una notación funcional:

\begin{equation}
g_i=t_i(\theta_i).
\end{equation}

El vocabulario semántico utilizado en esta investigación se define como:

\begin{equation}
\begin{split}
\mathcal{G}=\{&
\texttt{go},
\texttt{find},
\texttt{take},
\texttt{deliver},
\texttt{place},
\texttt{drop},\\
&
\texttt{guide},
\texttt{follow},
\texttt{count},
\texttt{tell},
\texttt{talk},
\texttt{save},\\
&
\texttt{answer\_question},
\texttt{greet}
\}.
\end{split}
\label{eq:vocabulario_goals}
\end{equation}

La Tabla~\ref{tab:vocabulario_goals} resume el propósito general de cada tipo
de objetivo y sus argumentos principales.

\begin{table}[ht]
\centering
\caption{Vocabulario de objetivos semánticos utilizado en la representación canónica.}
\label{tab:vocabulario_goals}
\begin{tabular}{|p{0.19\textwidth} | p{0.34\textwidth} | p{0.35\textwidth}|}
\hline
\textbf{Objetivo} & \textbf{Propósito} & \textbf{Argumentos principales} \\
\hline

\texttt{go}
& Desplazarse hacia una ubicación
& ubicación \\

\texttt{find}
& Localizar una persona u objeto
& objetivo, \texttt{kind}, gesto, pose, vestimenta o propiedad \\

\texttt{take}
& Tomar un objeto
& objeto \\

\texttt{deliver}
& Entregar un objeto
& objeto, destinatario \\

\texttt{place}
& Colocar un objeto en un destino
& objeto, destino y relación espacial \\

\texttt{drop}
& Depositar un objeto
& objeto, destino y relación espacial \\

\texttt{guide}
& Guiar a una persona
& persona, destino \\

\texttt{follow}
& Seguir a una persona
& persona, destino opcional \\

\texttt{count}
& Contar personas u objetos
& objetivo, \texttt{kind} y atributos opcionales \\

\texttt{tell}
& Comunicar información
& información o propiedad \\

\texttt{talk}
& Producir información verbal
& contenido \\

\texttt{save}
& Conservar información obtenida durante una interacción
& nombre, pose o gesto \\

\texttt{answer\_question}
& Atender una pregunta
& --- \\

\texttt{greet}
& Saludar a una persona
& persona \\

\hline
\end{tabular}
\end{table}

Los objetivos \texttt{find} y \texttt{count} requieren distinguir
explícitamente si operan sobre personas u objetos. Para ello se utiliza el
argumento \texttt{kind}:

\begin{verbatim}
find(Robin, kind=person)
find(apple, kind=object)

count(person, kind=person)
count(drinks, kind=object)
\end{verbatim}

Otros objetivos poseen un dominio semántico implícito. Por ejemplo,
\texttt{take}, \texttt{deliver}, \texttt{place} y \texttt{drop} operan sobre
objetos, mientras que \texttt{guide}, \texttt{follow} y \texttt{greet}
operan sobre personas.

Los objetivos también pueden conservar calificadores relevantes presentes en
la instrucción. Entre ellos se encuentran propiedades relacionadas con gestos,
poses, vestimenta, características de objetos y relaciones de destino.

Por ejemplo:

\begin{verbatim}
find(person, kind=person, gesture='waving person')

find(person, kind=person, pose='sitting person')

find(person, kind=person, wearing='red shirt')

find(drink, kind=object, property=largest)

guide(Robin, to=kitchen)

place(apple, on=table)
\end{verbatim}

La secuencia completa se serializa posteriormente mediante una estructura
JSON. Por ejemplo:

\begin{verbatim}
{
  "goals": [
    "go(living room)",
    "find(Robin, kind=person)",
    "follow(Robin, to=kitchen)"
  ]
}
\end{verbatim}

El formato de serialización no modifica la naturaleza conceptual de la
representación. El elemento relevante para la formalización es la secuencia
$G$, mientras que JSON constituye el mecanismo utilizado para intercambiar
dicha información entre componentes.


\subsubsection{Fidelidad de la representación semántica}
\label{subsubsec:fidelidad_semantica}

Una secuencia compuesta únicamente por objetivos válidos no constituye
necesariamente una interpretación correcta de una instrucción.

La representación canónica debe conservar las intenciones relevantes de la
orden y eliminar únicamente la variabilidad superficial que no modifica su
significado dentro del dominio.

Considérese la instrucción:

\begin{quote}
\textit{``Go to the kitchen and find Robin.''}
\end{quote}

La secuencia esperada es:

\begin{verbatim}
go(kitchen)
find(Robin, kind=person)
\end{verbatim}

Una salida como:

\begin{verbatim}
find(Robin, kind=person)
\end{verbatim}

utiliza un objetivo válido, pero omite una subtarea solicitada explícitamente
por la orden.

De manera similar:

\begin{verbatim}
go(kitchen)
find(Robin, kind=person)
greet(Robin)
\end{verbatim}

incorpora una intención adicional que no forma parte de la instrucción
original.

Por tanto, una interpretación correcta debe satisfacer, conceptualmente, las
siguientes propiedades:

\begin{itemize}

    \item preservar las subtareas solicitadas;

    \item no introducir objetivos ajenos a la instrucción;

    \item conservar las entidades relevantes;

    \item conservar las relaciones entre dichas entidades;

    \item mantener las restricciones de orden necesarias para completar la
    tarea.

\end{itemize}

Si se dispone de una secuencia de referencia $G_{\mathrm{ref}}$, una condición
ideal de interpretación puede expresarse como:

\begin{equation}
G_{\mathrm{pred}} = G_{\mathrm{ref}},
\end{equation}

donde $G_{\mathrm{pred}}$ representa la interpretación producida por el modelo.

Esta igualdad constituye una condición estricta y posteriormente puede
descomponerse en métricas más específicas para analizar qué componentes de la
interpretación fueron correctos o incorrectos.


% ------------------------------------------------------------
\subsection{Objetivos semánticos y acciones ejecutables}
\label{subsec:objetivos_acciones}

Los objetivos canónicos describen \textbf{qué} debe conseguir el robot, pero no
necesariamente especifican directamente todas las operaciones requeridas para
conseguirlo.

Sea $\mathcal{G}$ el vocabulario de objetivos semánticos definido
anteriormente y sea:

\begin{equation}
\mathcal{A}
\end{equation}

el repertorio de acciones disponibles para el sistema de ejecución.

En esta investigación:

\begin{equation}
\boxed{
\mathcal{G}\neq\mathcal{A}.
}
\label{eq:goals_acciones_distintas}
\end{equation}

Entre las acciones utilizadas por el ejecutor se encuentran, por ejemplo:

\begin{equation}
\begin{split}
\mathcal{A}\supseteq\{&
\texttt{go\_to},
\texttt{say},
\texttt{listen},
\texttt{find\_person},
\texttt{find\_pose},\\
&
\texttt{approach},
\texttt{focus},
\texttt{find\_object},
\texttt{analyze\_objects},\\
&
\texttt{analyze\_person},
\texttt{take},
\texttt{find\_space},
\texttt{drop},
\texttt{give}
\}.
\end{split}
\end{equation}

Algunos elementos pueden poseer nombres similares en ambos vocabularios, como
\texttt{take}, pero esto no implica que ambas representaciones sean
equivalentes en general.

Por ejemplo, el objetivo semántico:

\begin{verbatim}
deliver(apple, to=Robin)
\end{verbatim}

representa una intención de alto nivel. Su realización puede requerir varias
operaciones, como localizar al destinatario, aproximarse y ejecutar la entrega
mediante la acción correspondiente.

Conceptualmente:

\begin{equation}
\texttt{deliver}(o,p)
\rightarrow
(
\texttt{find\_person},
\texttt{approach},
\texttt{give},
\ldots
).
\end{equation}

De manera similar:

\begin{verbatim}
place(apple, on=table)
\end{verbatim}

puede requerir identificar un espacio adecuado antes de ejecutar la acción de
liberar el objeto:

\begin{equation}
\texttt{place}(o,l)
\rightarrow
(
\texttt{find\_space},
\texttt{drop},
\ldots
).
\end{equation}

La secuencia completa de acciones se define como:

\begin{equation}
P=(a_1,a_2,\ldots,a_m),
\end{equation}

donde:

\begin{equation}
a_i\in\mathcal{A}.
\end{equation}

Cada objetivo puede producir una subsecuencia de acciones:

\begin{equation}
g_i
\rightarrow
(a_j,a_{j+1},\ldots,a_k).
\end{equation}

Por tanto, el problema de planificación considerado puede expresarse de manera
general como una transformación:

\begin{equation}
R:
\mathcal{G}^{*}
\rightarrow
\mathcal{A}^{*},
\end{equation}

de forma que:

\begin{equation}
R(G)=P.
\end{equation}

Esta distinción es fundamental para las arquitecturas comparadas en esta
investigación. La etapa de interpretación produce elementos pertenecientes a
$\mathcal{G}$, mientras que el resultado final de planificación debe estar
compuesto por elementos pertenecientes a $\mathcal{A}$.


% ------------------------------------------------------------
\subsection{Dependencias y contexto entre objetivos}
\label{subsec:dependencias_objetivos}

La secuencia $G$ no puede tratarse únicamente como un conjunto independiente de
objetivos. Algunas subtareas presentan relaciones de precedencia, referencias
compartidas o condiciones que dependen del resultado de objetivos anteriores.

Por este motivo:

\begin{equation}
G=(g_1,g_2,\ldots,g_n)
\end{equation}

se considera una \textbf{secuencia ordenada de objetivos} y no simplemente un
conjunto:

\begin{equation}
G\neq\{g_1,g_2,\ldots,g_n\}.
\end{equation}


\subsubsection{Dependencias de precedencia}
\label{subsubsec:dependencia_precedencia}

Algunos objetivos requieren que otra subtarea haya sido realizada
previamente.

Por ejemplo:

\begin{verbatim}
take(apple)
deliver(apple, to=Robin)
\end{verbatim}

requiere que el objeto haya sido adquirido antes de intentar entregarlo.

Esta relación puede representarse como:

\begin{equation}
\texttt{take}(o)
\prec
\texttt{deliver}(o,p),
\end{equation}

donde $\prec$ representa una relación de precedencia.

De manera equivalente:

\begin{equation}
\texttt{take}(o)
\prec
\texttt{place}(o,l).
\end{equation}

Una secuencia que invirtiera estas relaciones podría contener objetivos
individualmente válidos, pero no representaría una composición coherente de la
tarea.


\subsubsection{Dependencias referenciales}
\label{subsubsec:dependencia_referencial}

Algunas instrucciones introducen entidades que posteriormente deben ser
reutilizadas.

Considérese:

\begin{verbatim}
find(person, kind=person, gesture='waving person')
guide(person, to=kitchen)
\end{verbatim}

El argumento \texttt{person} utilizado en el segundo objetivo no representa una
persona arbitraria, sino la entidad identificada como resultado del objetivo
anterior.

Conceptualmente, esta relación puede expresarse como:

\begin{equation}
ref(g_j)=result(g_i),
\qquad i<j,
\end{equation}

donde $result(g_i)$ representa la entidad obtenida como resultado de una
subtarea anterior.

Estas relaciones permiten representar expresiones lingüísticas que utilizan
pronombres, referencias genéricas o descripciones cuyo referente se determina
durante la ejecución de la tarea.


\subsubsection{Dependencias espaciales}
\label{subsubsec:dependencia_espacial}

La interpretación de una subtarea también puede depender del contexto espacial
establecido por objetivos anteriores.

Por ejemplo:

\begin{verbatim}
go(kitchen)
find(apple, kind=object)
\end{verbatim}

establece que la búsqueda del objeto debe realizarse después de desplazarse
hacia la cocina.

En este caso, el primer objetivo establece el contexto espacial esperado para
la ejecución del segundo.

De manera conceptual, puede considerarse que determinados objetivos producen
una actualización del estado esperado:

\begin{equation}
S_{i+1}=T(S_i,g_i),
\label{eq:estado_semantico}
\end{equation}

donde $S_i$ representa un estado abstracto antes de procesar el objetivo
$g_i$ y $T$ una función conceptual de transición.

Así, un objetivo:

\begin{equation}
\texttt{go}(l)
\end{equation}

puede producir conceptualmente:

\begin{equation}
location(S_{i+1})=l.
\end{equation}

Esta representación no implica que el movimiento físico haya sido ejecutado
con éxito; únicamente representa el estado esperado utilizado durante la
composición del plan.


\subsubsection{Dependencias de estado}
\label{subsubsec:dependencia_estado}

Algunas subtareas modifican condiciones que son necesarias para objetivos
posteriores.

Por ejemplo:

\begin{verbatim}
take(apple)
deliver(apple, to=Robin)
\end{verbatim}

establece conceptualmente un estado intermedio en el que el robot posee el
objeto:

\begin{equation}
holding(S_{i+1})=apple.
\end{equation}

El objetivo posterior de entrega presupone esta condición.

De esta forma, las relaciones entre objetivos no dependen exclusivamente de su
posición dentro de la secuencia, sino también del estado esperado producido por
las subtareas anteriores.


\subsubsection{Orden lingüístico y orden semántico}
\label{subsubsec:orden_semantico}

Finalmente, debe distinguirse entre el orden superficial en que aparecen los
elementos dentro de una oración y el orden lógico requerido para realizar la
tarea.

Por ejemplo:

\begin{quote}
\textit{``Bring me the apple from the kitchen.''}
\end{quote}

expresa directamente la intención de entrega, pero para realizarla es
necesario construir una secuencia semántica como:

\begin{verbatim}
go(kitchen)
find(apple, kind=object)
take(apple)
deliver(apple, to=me)
\end{verbatim}

Por tanto, la interpretación no consiste en copiar el orden de aparición de las
palabras, sino en recuperar las relaciones necesarias entre las subtareas.

Para este ejemplo:

\begin{equation}
\texttt{go}
\prec
\texttt{find}
\prec
\texttt{take}
\prec
\texttt{deliver}.
\end{equation}

Esta propiedad resulta especialmente importante en instrucciones compactas,
donde algunas operaciones necesarias para cumplir la intención pueden no
aparecer expresadas como verbos independientes.

En consecuencia, la representación semántica $G$ debe conservar tanto la
intención de la orden como las relaciones necesarias para que sus objetivos
puedan ser posteriormente transformados en un plan coherente.

La formalización desarrollada en esta sección permite resumir el problema
considerado mediante:

\begin{equation}
\boxed{
x
\xrightarrow{I}
G
\xrightarrow{R}
P
}
\end{equation}

con:

\begin{equation}
x\in\mathcal{X},
\qquad
G\in\mathcal{G}^{*},
\qquad
P\in\mathcal{A}^{*}.
\end{equation}

La transformación $I$ abstrae la variabilidad del lenguaje natural y conserva
la intención mediante una representación canónica, mientras que $R$ transforma
dicha representación en una secuencia de acciones pertenecientes al repertorio
del robot. Las dos arquitecturas estudiadas en esta tesis se diferencian
principalmente en la manera en que implementan estas transformaciones, como se
describe en la sección siguiente.


% ============================================================
\section{Arquitecturas conceptuales evaluadas}
\label{sec:arquitecturas_conceptuales}

A partir de la formalización anterior, las dos arquitecturas estudiadas pueden
describirse de acuerdo con la manera en que realizan las transformaciones:

\begin{equation}
x\rightarrow G
\end{equation}

y:

\begin{equation}
G\rightarrow P.
\end{equation}

Las arquitecturas se evalúan de forma independiente y no existe un mecanismo
de selección, votación o fusión entre los planes producidos.


\subsection{Arquitectura Qwen--CLIPS}
\label{subsec:qwen_clips}

La primera arquitectura divide explícitamente las funciones de interpretación
y planificación.

Qwen2.5-7B recibe una orden $x$ y produce la secuencia canónica de objetivos:

\begin{equation}
I_Q:x\rightarrow G,
\end{equation}

donde $I_Q$ representa el intérprete semántico basado en Qwen.

La secuencia resultante se somete a comprobaciones deterministas antes de ser
entregada al componente simbólico. CLIPS recibe posteriormente la
representación de dichos objetivos y aplica las reglas correspondientes para
producir el plan:

\begin{equation}
R_C:G\rightarrow P,
\end{equation}

donde $R_C$ representa la expansión basada en reglas.

Conceptualmente, la arquitectura puede expresarse como:

\begin{equation}
\boxed{
x
\xrightarrow{\text{Qwen}}
G
\xrightarrow{\text{validación y adaptación}}
G'
\xrightarrow{\text{CLIPS}}
P
}
\label{eq:qwen_clips}
\end{equation}

En esta arquitectura, Qwen no genera las acciones primitivas del robot. Su
responsabilidad se limita a interpretar la instrucción y producir los objetivos
correspondientes. La forma en que cada objetivo se transforma en acciones se
encuentra definida en la base de reglas del componente simbólico.

La separación permite distinguir conceptualmente dos posibles fuentes de
error:

\begin{equation}
x\rightarrow G
\qquad
\text{e}
\qquad
G\rightarrow P.
\end{equation}

Un plan incorrecto puede originarse porque Qwen interpretó incorrectamente la
orden o porque la etapa simbólica no produjo una expansión adecuada para los
objetivos recibidos.

La arquitectura se considera local o autoalojada en el sentido de que el
modelo Qwen y el componente CLIPS pueden ejecutarse dentro de infraestructura
controlada por el sistema, sin requerir un servicio externo de inferencia para
cada orden. La configuración física concreta empleada durante los experimentos
se describe en el capítulo de implementación.


\subsection{Arquitectura de planificación directa con GPT}
\label{subsec:gpt_directo}

La segunda arquitectura concentra las funciones de interpretación y
planificación en un mismo modelo.

Para una orden considerada ejecutable, GPT produce tanto una representación
canónica de los objetivos como el plan propuesto:

\begin{equation}
D_{\mathrm{GPT}}:
(x,K,\mathcal{G},\mathcal{A})
\rightarrow
(G,P),
\end{equation}

donde $K$ representa el conocimiento del dominio, $\mathcal{G}$ el vocabulario
de objetivos permitido y $\mathcal{A}$ el repertorio de acciones disponibles.

Conceptualmente:

\begin{equation}
\boxed{
x
\xrightarrow{\text{GPT}}
(G,P)
\xrightarrow{\text{validación}}
(G',P')
}
\label{eq:gpt_directo}
\end{equation}

Los objetivos $G$ se conservan como representación auditable de la
interpretación realizada por el modelo. Sin embargo, a diferencia de la
arquitectura anterior, no son enviados a CLIPS.

Por tanto:

\begin{equation}
G_{\mathrm{GPT}}
\not\xrightarrow{\text{CLIPS}}
P.
\end{equation}

El mismo modelo debe interpretar la orden y determinar la expansión necesaria
para producir acciones pertenecientes a $\mathcal{A}$.

Esta característica permite comparar dos formas diferentes de distribuir la
responsabilidad del proceso:

\begin{equation}
\boxed{
\begin{array}{ll}
\text{Qwen--CLIPS:}
&
x \xrightarrow{\text{Qwen}} G
\xrightarrow{\text{CLIPS}} P
\\[0.4cm]
\text{GPT directo:}
&
x \xrightarrow{\text{GPT}} (G,P)
\end{array}
}
\end{equation}

En ambos casos se conserva una representación semántica de los objetivos, lo
que permite evaluar posteriormente la interpretación de forma separada del
plan generado.

El contrato completo utilizado por GPT, incluida la posibilidad de solicitar
una aclaración o rechazar una instrucción que no pueda convertirse de manera
válida en un plan, se desarrolla en los capítulos de metodología e
implementación.


% ============================================================
\section{Criterios conceptuales de validez}
\label{sec:criterios_validez}

La generación de una representación estructurada no implica por sí misma que
la interpretación o el plan sean correctos. Como se explicó en el capítulo
anterior, la conformidad con un formato constituye solamente uno de los niveles
de validación.

Para la formalización de esta investigación resulta útil distinguir la validez
de la interpretación de la validez del plan.


\subsection{Validez de la interpretación semántica}
\label{subsec:validez_interpretacion}

Sea:

\begin{equation}
V_I(x,G,K)
\end{equation}

una función que evalúa si la secuencia $G$ representa adecuadamente la
instrucción $x$ dentro del conocimiento de dominio $K$.

Conceptualmente:

\begin{equation}
V_I(x,G,K)
\in
\{0,1\}.
\end{equation}

Una interpretación válida debe conservar, entre otros elementos:

\begin{itemize}

    \item las subtareas solicitadas por la orden;
    \item el orden lógico de dichas subtareas;
    \item las entidades involucradas;
    \item sus tipos semánticos;
    \item las ubicaciones y destinos;
    \item los calificadores relevantes;
    \item las relaciones entre referencias presentes en la instrucción.

\end{itemize}

Por ejemplo, para:

\begin{quote}
\textit{``Go to the kitchen and find Robin.''}
\end{quote}

una representación esperada es:

\begin{verbatim}
go(kitchen)
find(Robin, kind=person)
\end{verbatim}

Una salida estructuralmente correcta que omitiera la navegación, cambiara a
Robin por otra persona o clasificara a Robin como objeto no representaría
correctamente la intención original.

Las métricas concretas utilizadas para determinar esta validez se presentan
posteriormente en el capítulo de experimentación.


\subsection{Validez y consistencia del plan}
\label{subsec:validez_plan}

Sea:

\begin{equation}
V_P(G,P,K,\mathcal{A})
\end{equation}

una función destinada a evaluar si un plan $P$ constituye una expansión
coherente de los objetivos $G$.

De manera conceptual:

\begin{equation}
V_P(G,P,K,\mathcal{A})
\in
\{0,1\}.
\end{equation}

Un plan válido debe utilizar acciones pertenecientes al repertorio permitido y
mantener consistencia con los objetivos que pretende satisfacer.

Entre las propiedades relevantes se encuentran:

\begin{itemize}

    \item utilización de acciones pertenecientes a $\mathcal{A}$;
    \item parámetros compatibles con cada acción;
    \item conservación del orden y las dependencias necesarias;
    \item inclusión de las subtareas requeridas;
    \item coherencia entre las entidades utilizadas en objetivos y acciones;
    \item ausencia de operaciones incompatibles con el estado esperado de la
    tarea.

\end{itemize}

Esta separación entre interpretación y planificación permite analizar los
errores de manera más precisa.

Puede ocurrir:

\begin{equation}
V_I(x,G,K)=0,
\end{equation}

aunque la expansión de $G$ sea internamente consistente. En este caso, el
problema se encuentra principalmente en la interpretación de la orden.

También puede ocurrir:

\begin{equation}
V_I(x,G,K)=1
\qquad\text{y}\qquad
V_P(G,P,K,\mathcal{A})=0,
\end{equation}

lo que indica que la intención fue identificada correctamente pero el plan
generado presenta un problema.

Esta distinción motiva además la utilización posterior de una condición
experimental de control en la que los objetivos de referencia son
proporcionados directamente a CLIPS:

\begin{equation}
\boxed{
G_{\mathrm{ref}}
\xrightarrow{\text{CLIPS}}
P_{\mathrm{ref}}
}
\end{equation}

Esta condición no constituye una tercera arquitectura de planificación. Su
función es permitir observar el comportamiento del componente simbólico cuando
la entrada semántica se considera correcta, facilitando la separación entre
errores de interpretación y errores de planificación.


\subsection{Planificación y ejecución física}
\label{subsec:planificacion_ejecucion}

Finalmente, debe distinguirse entre la validez de un plan y el resultado de su
ejecución física.

El planificador trabaja con una representación de las acciones que se espera
que el robot pueda realizar. Sin embargo, la ejecución se desarrolla en un
entorno físico en el que pueden presentarse condiciones que no fueron
consideradas durante la generación del plan.

Por tanto:

\begin{equation}
\boxed{
\text{plan válido}
\not\Rightarrow
\text{ejecución física exitosa}
}
\end{equation}

Por ejemplo, un plan puede contener correctamente una acción destinada a tomar
un objeto, pero la ejecución puede fallar porque el objeto no fue detectado, no
se encuentra en la posición esperada o el manipulador no logra sujetarlo.

De manera similar, una acción de navegación puede ser correcta desde el punto
de vista del plan y no completarse debido a cambios en el entorno o a una
trayectoria bloqueada.

Esta distinción implica que los criterios utilizados para evaluar la
interpretación y la planificación no sustituyen la evaluación del desempeño
físico del robot. La ejecución sobre Justina constituye un nivel adicional de
validación que se describe posteriormente en el diseño experimental.

En consecuencia, el problema central de esta tesis queda definido mediante
tres representaciones:

\begin{equation}
\boxed{
x
\rightarrow
G
\rightarrow
P
\rightarrow
\text{ejecución}
}
\end{equation}

Las dos arquitecturas evaluadas difieren principalmente en el mecanismo
utilizado para realizar las dos primeras transformaciones. Qwen--CLIPS separa
la interpretación lingüística de la expansión simbólica del plan, mientras que
GPT directo concentra ambas funciones en un único modelo generativo. Esta
diferencia proporciona la base conceptual para el diseño metodológico y la
comparación experimental desarrollados en los capítulos siguientes.